{
    "name": "collab-client-js",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "typecheck": "tsc --noEmit",
        "dev:local": "vite --config vite.config.local.ts",
        "build:local": "vite build --config vite.config.local.ts",
        "pretypecheck:local": "node ../../scripts/check-local-survey.mjs",
        "typecheck:local": "tsc -p tsconfig.local.json --noEmit"
    },
    "dependencies": {
        "survey-core": "^3.0.1",
        "survey-js-ui": "^3.0.1",
        "survey-creator-core": "^3.0.1",
        "survey-creator-js": "^3.0.1"
    },
    "devDependencies": {
        "typescript": "^5.5.4",
        "vite": "^5.4.2"
    }
}
